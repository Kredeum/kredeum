{
  "extends": ["//"],
  "pipeline": {
    "check": {
      "inputs": ["src/**/*.json"]
    },
    "build": {
      "dependsOn": ["check"],
      "inputs": ["src/*.json"],
      "outputs": ["dist/*.json"]
    }
  }
}
