{
  "extends": ["//"],
  "pipeline": {
    "check": {
      "inputs": ["src/**/*.json"]
    },
    "build": {
      "dependsOn": ["check"],
      "inputs": ["src/*.json", "scripts/*.ts"],
      "outputs": ["dist/*.json"]
    }
  }
}
