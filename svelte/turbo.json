{
  "extends": ["//"],
  "pipeline": {
    "build": {
      "dependsOn": ["@kredeum/gulp#build", "@kredeum/config#build", "@kredeum/contracts#deploy"],
      "inputs": ["**/*.ts", "**/*.svelte"],
      "outputs": ["web/dapp/assets/js/*"]
    },
    "dev": {
      "dependsOn": ["@kredeum/gulp#build", "@kredeum/config#build", "@kredeum/contracts#deploy"],
      "cache": false,
      "persistent": true
    }
  }
}
