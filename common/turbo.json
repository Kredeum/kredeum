{
  "extends": ["//"],
  "pipeline": {
    "check": {
      "dependsOn": ["format", "@kredeum/config#build", "@kredeum/gulp#build"],
      "inputs": ["src/*", "../contracts/addresses.json"]
    }
  }
}
