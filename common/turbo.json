{
  "extends": ["//"],
  "pipeline": {
    "check": {
      "dependsOn": ["format", "@kredeum/config#build", "@kredeum/gulp#build", "@kredeum/contracts#build"],
      "inputs": ["src/*", "../contracts/addresses.json"]
    }
  }
}
