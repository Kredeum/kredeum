{
  "name": "kredeum",
  "version": "1.4.0",
  "main": "index.js",
  "scripts": {
    "clean": "rm -rf node_modules pnpm-lock.yaml web/dapp **/.turbo",
    "all": "pnpm clean && pnpm -r clean && pnpm install && turbo build",
    "build:dapp": "turbo build -F sveltekit",
    "deploy:dapp": "pnpm install && pnpm -F contracts install:foundry && pnpm build:dapp",
    "deploy:plugin": "pnpm install && pnpm install-wordpress && pnpm build-plugin"
  },
  "keywords": [
    "Kredeum",
    "NFT",
    "Factory"
  ],
  "author": "kredeum",
  "license": "MIT",
  "engines": {
    "node": "^20",
    "pnpm": "^8"
  },
  "devDependencies": {
    "turbo": "^1.12.3"
  }
}
