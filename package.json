{
  "name": "kredeum",
  "version": "1.4.0",
  "main": "index.js",
  "scripts": {
    "clean": "rm -rf node_modules pnpm-lock.yaml .turbo web/dapp && pnpm -r clean",
    "all": "pnpm clean && pnpm install && pnpm build",
    "all:dapp": "pnpm clean && pnpm install && pnpm build:dapp",
    "all:plugin": "pnpm clean && pnpm install && pnpm build:plugin",
    "build": "turbo build",
    "build:dapp": "turbo build -F sveltekit",
    "build:plugin": "turbo build -F wordpress",
    "deploy:dapp": "pnpm install && pnpm -F contracts install:foundry && pnpm build:dapp",
    "deploy:plugin": "pnpm install && pnpm -F wordpress install:wordpress && pnpm build:plugin"
  },
  "keywords": [
    "Kredeum",
    "NFT",
    "Factory"
  ],
  "author": "kredeum",
  "license": "MIT",
  "engines": {
    "node": "^20",
    "pnpm": "^8"
  },
  "devDependencies": {
    "turbo": "^1.12.4"
  }
}
