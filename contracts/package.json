{
  "name": "@kredeum/contracts",
  "version": "1.4.0",
  "author": "Kredeum",
  "description": "",
  "devDependencies": {
    "@ethersproject/abi": "^5.7.0",
    "@ethersproject/providers": "^5.7.2",
    "@typechain/ethers-v5": "^11.1.2",
    "ethers": "^5.7.2",
    "solc": "0.8.19",
    "solhint": "^4.1.1",
    "solidity-coverage": "^0.8.7",
    "ts-node": "^10.9.2",
    "tsconfig-paths": "^4.2.0",
    "typechain": "^8.3.2",
    "typescript": "^5.3.3"
  },
  "directories": {
    "test": "tests"
  },
  "keywords": [],
  "license": "ISC",
  "main": "index.js",
  "scripts": {
    "clean": "forge clean && rm -rf cache-forge out && rm -rf node_modules lib types artifacts broadcast",
    "install:foundry": "curl -L https://foundry.paradigm.xyz | bash && $HOME/.foundry/bin/foundryup && pnpm foundry:init && pnpm foundry:update",
    "build": "pnpm build:forge && pnpm build:typechain",
    "build:forge": "$HOME/.foundry/bin/forge build",
    "build:typechain": "pnpx typechain --target ethers-v5 --out-dir types out/**/*.json",
    "check": "pnpm check:solidity",
    "check:solidity": "solhint src/**/*.sol",
    "deploy:deploy": "pnpm deploy:all --fork-url $CHAIN --sender $SENDER --account $ACCOUNT --broadcast",
    "deploy:validate": "pnpm deploy:all --fork-url $CHAIN --sender $SENDER",
    "deploy:testnet": "SENDER=$TESTNET_SENDER ACCOUNT=$TESTNET_ACCOUNT pnpm deploy:deploy",
    "deploy:mainnet": "SENDER=$MAINNET_SENDER ACCOUNT=$MAINNET_ACCOUNT pnpm deploy:deploy --verify",
    "deploy:anvil": "export CHAIN=anvil SENDER=$ANVIL_SENDER ACCOUNT=$ANVIL_ACCOUNT  && pnpm deploy:deploy && pnpm deploy:validate",
    "deploy:sepolia": "CHAIN=sepolia pnpm deploy:testnet",
    "deploy:all": "forge script scripts/DeployAllCurrent.s.sol",
    "validate:only": "pnpm deploy:all --sender $SENDER",
    "format": "pnpm format:solidity",
    "format:solidity": "$HOME/.foundry/bin/forge fmt",
    "foundry:init": "$HOME/.foundry/bin/forge init --force --no-commit && rm -rf **/Counter*.sol",
    "foundry:update": "git submodule update --recursive",
    "foundry:types": "pnpx typechain --out-dir types --target ethers-v5  out/**/*.json",
    "tests": "pnpm tests:forge",
    "tests:forge": "forge test"
  }
}